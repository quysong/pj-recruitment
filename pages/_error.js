import React from 'react';
import PropTypes from 'prop-types';

const Error = props => {
    return (
        <div>
            404 Pages
        </div>
    );
};

Error.propTypes = {
    props: PropTypes.any
};

export default Error;